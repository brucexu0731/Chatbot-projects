fetch() is how you make HTTP requests
from the browser.

You need to specify:

- the URL ("/api/gift")
- the method ("POST")
- the headers (Content-Type: application/json)
- the body (your prompt, as JSON)

If this feels unfamiliar:
https://scrimba.com/links/mdn-fetch
