The OpenAI call on the server
looks exactly like it did on the client.

Same SDK.
Same method.
Same messages array.

The only difference:
no dangerouslyAllowBrowser needed.
That was a workaround for the browser.
On the server, it's unnecessary.
